# Python
This graph represents the comparison between the actual human distance and the distance measured using an ultrasonic sensor. The x-axis shows the actual distance of a human from the sensor (in cm), which was measured using a standard measuring tape. The y-axis represents the distance recorded by the ultrasonic sensor. An ideal measurement 
